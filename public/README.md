# Public assets

## storefront.jpg (hero background — drop slot)

The hero section references `/storefront.jpg`. Save the real storefront photo
(IMG_6897.jpg) here as `storefront.jpg` and it will appear automatically behind
the hero. Until then, a charcoal gradient + texture stands in — no broken image,
just a darker hero.

Recommended: ~1920px wide, optimized JPG/WebP under ~400KB.
